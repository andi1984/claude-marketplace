# Validate Marketplace

Validate the marketplace configuration and all plugins.

## Instructions

1. Check that `.claude-plugin/marketplace.json` is valid JSON
2. Verify all referenced plugins exist in the `plugins/` directory
3. Check each plugin has required files (manifest, entry point)
4. Report any issues found

$ARGUMENTS
